# FastAPI & server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# PDF extraction (local)
pymupdf>=1.24.0          # import as: import fitz

# AWS SDK (Bedrock + future Textract)
boto3>=1.34.0
google-genai>=0.8.3

# Data validation
pydantic>=2.7.0

# Dev / testing (optional, not needed at runtime)
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
